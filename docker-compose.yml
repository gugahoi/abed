services:
  movie-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: movie-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/requests.db
    volumes:
      - ./data:/app/data
    networks:
      - media

networks:
  media:
    external: true
    name: media
